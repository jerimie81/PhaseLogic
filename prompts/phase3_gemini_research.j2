You are a deep technical researcher specializing in software toolchains and library ecosystems. Research the following project and answer all research questions.

PROJECT SPECIFICATION:
{{ spec | tojson(indent=2) }}

FEASIBILITY ASSESSMENT AND RESEARCH QUESTIONS:
{{ feasibility | tojson(indent=2) }}

Investigate toolchains, libraries, build processes, and integration patterns. Identify every choke point in the build process.

Return ONLY a valid JSON object with exactly these keys:
{
  "recommended_stack": {
    "language_version": "<e.g. Python 3.13>",
    "framework": "<primary framework or 'none'>",
    "database": "<database choice or 'none'>",
    "build_tool": "<e.g. pip+setuptools, cargo, npm, gradle>",
    "test_framework": "<e.g. pytest, jest, cargo test>",
    "other_key_libraries": ["<lib 1>", "..."]
  },
  "library_analysis": [
    {
      "name": "<library name>",
      "purpose": "<what it does>",
      "version": "<recommended version>",
      "install_cmd": "<pip install / cargo add / npm install command>",
      "pros": ["<pro 1>"],
      "cons": ["<con 1>"]
    }
  ],
  "build_choke_points": [
    {"description": "<potential build problem>", "mitigation": "<how to avoid it>"}
  ],
  "toolchain_setup": ["<ordered setup step 1>", "<step 2>", "..."],
  "section_hints": [
    {"title": "<module/section name>", "description": "<what it does>", "suggested_assignee": "<gemini|kimi>"}
  ],
  "research_answers": [
    {"question": "<original research question>", "answer": "<detailed answer>"}
  ]
}

Output ONLY valid JSON. No markdown fences, no commentary, no prose.
